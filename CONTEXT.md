# AI Notes

此文件記錄 `AI_notes/` 這個 Obsidian vault 的共同語言，讓人與 AI 在整理筆記時使用同一套詞彙。

## 共同語言

### 知識卡

沉澱理解後的卡片，只處理一個核心想法。知識卡應使用自己的話重寫，保留足夠推理脈絡，避免變成單篇主題總整理。

### 原始資料卡

保留單一來源脈絡的卡片。它可以較長，重點是讓之後能追溯某個想法來自哪篇文章、文件、影片、課程或對話，並連回由它沉澱出的知識卡。

### 索引卡

主題入口與導航卡。索引卡負責整理某個主題下有哪些相關知識卡與原始資料卡，不負責完整解釋主題。

### 類型標籤

每張卡片第一行必須有且只有一個卡片類型標籤：`#知識卡`、`#原始資料卡`、`#索引卡`。類型標籤用來區分筆記功能，不取代雙向連結。

### 系統文件

描述規則、共同語言或 vault 入口的文件，例如 `AGENTS.md`、`CONTEXT.md`、`README.md`。系統文件不是卡片，不需要卡片類型標籤，也可以使用 H1 標題。

### 變更後檢查

新增或調整筆記後，用來確認卡片類型、標籤、格式、來源、連結與 diff 是否符合 vault 規則的一組檢查。它不是新的卡片類型，而是維護筆記品質的流程。

### 空白筆記

沒有實際正文內容的筆記，或只剩標籤列但缺少卡片內容的筆記。空白筆記通常代表建立未完成、批次修改失敗或內容被誤刪，變更後必須檢查並修正。

### 檔案讀寫與編碼

讀取與修改 Markdown 筆記時使用 UTF-8 並避免整檔重寫造成中文亂碼或內容截斷的維護規則。終端顯示亂碼不一定代表檔案損壞，必須用 UTF-8 讀取或 diff 交叉確認。
