# AGENTS.md

本規則只適用於 `AI 相關筆記/` 及其子資料夾。

## 核心原則

在 `AI 相關筆記/` 建立筆記時，必須以「卡片盒筆記法」建立。

筆記應該是經過理解後的知識節點，而不是資料堆放、原文摘錄或單篇長文整理。除非使用者明確要求保留原文，否則必須用自己的話重寫。

使用者當下的明確指令優先於本檔案的預設規則。例如使用者明確要求建立單篇筆記時，以單篇為準，但仍盡量保留卡片盒精神。

## 建立前

建立新筆記前，先搜尋 `AI 相關筆記/` 中的既有相關筆記，判斷是否有可連結的索引卡或原子卡。

若新筆記與既有筆記主題高度接近，先詢問使用者要更新既有筆記，還是建立新筆記。

若新名詞、別名或使用者要求新增的項目，已經有等價的既有筆記或索引卡，直接連回既有筆記，不要建立重複筆記，也不要在名詞清單中新增語意重複的條目。必要時可在既有筆記中補充別名說明。

若使用者沒有指定資料夾，一律先詢問，不要自行決定存放位置。

## 筆記拆分

預設永遠拆成多張卡片，而不是把整個主題寫成單篇長文。

每個主題至少建立一張索引卡，並將每個獨立概念拆成一張原子卡。

每張原子卡只回答一個核心想法，長度以 150-300 字為原則。必要時可以略長，但不要混入多個獨立概念。

檔名使用自然語句式標題，不使用流水號、時間戳或抽象 ID。

Obsidian 已經會以檔名呈現筆記標題，因此筆記正文第一行不要再重複放 H1 標題。正文從標籤列開始。

## 索引卡格式

索引卡負責提供主題入口與導航，不負責完整解釋。

索引卡固定包含：

```markdown
標籤：#AI #主題 #索引筆記

## 核心問題

這個主題主要回答什麼問題？

## 相關卡片

- [[相關原子卡 A]]
- [[相關原子卡 B]]

## 初步理解

用 2-4 句話說明這個主題的整體輪廓。
```

## 原子卡格式

原子卡固定包含 `## 一句話`、`## 內容`、`## 連結`。

原子卡格式：

```markdown
標籤：#AI #主題 #相關標籤

## 一句話

用一句話說清楚這張卡片的核心主張。

## 內容

用自己的話說明這個概念。只處理一個核心想法，避免寫成主題總整理。

## 連結

- [[主題索引卡]]
- [[相關原子卡]]
```

每張原子卡的 `## 連結` 至少要連回索引卡，並盡量連到 1-3 張相關卡。

`## 我的理解`、`## 延伸思考` 等區塊不固定要求，只在內容需要時加入。

## 標籤

不使用 YAML frontmatter。

每張筆記都要有正文標籤列：

```markdown
標籤：#AI #Codex #skill
```

每張筆記的標籤控制在 2-5 個。知識關聯主要依靠 `[[雙向連結]]`，不要用大量標籤取代連結。

## 來源

有明確來源時加入 `## 來源`；沒有來源時不用硬加。

來源可以是官方文件、課程、影片、對話、書籍或網頁。

```markdown
## 來源

- [來源名稱](URL)
- 課程名稱 / 對話日期 / 文件名稱
```

## 連結維護

建立新筆記後，若已有明確相關索引卡，應同步更新該索引卡的 `## 相關卡片`。

新增索引卡時，也應連到相關既有索引卡或原子卡。

不要建立孤立筆記。卡片盒的重點不是拆開，而是讓新想法接到既有知識網絡。

## 語言

筆記以中文為主，保留必要英文術語。

例如 `skill`、`AI agent`、`MCP`、`harness`、`AGENTS.md`、`SKILL.md` 等術語可以保留英文，不需要硬翻譯。
